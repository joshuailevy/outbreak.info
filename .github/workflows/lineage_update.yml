name: Get new lineages from pango

on:
   schedule:
     - cron: '55 * * * *'

jobs:
    scheduled_check:
       runs-on: ubuntu-20.04
       steps:
         - name: process data
           run: echo "running python script"
         - name: commit if output is different 
           run: echo "running git diff, committing if different"